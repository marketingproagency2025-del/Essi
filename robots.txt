# robots.txt — https://www.marketingpro-agency.com/
# MarketingPro Digital Marketing Agency
#
# NOTE FOR ANYONE EDITING THIS FILE
# A named user-agent group REPLACES the `*` group for that crawler; the rules do
# not merge. So every Disallow has to exist in BOTH groups below, or it silently
# will not apply to the AI crawlers. That is why the eight AI agents share one
# group instead of having eight of their own: two places to keep in sync, not nine.

# Default: allow all crawlers to index the whole site.
User-agent: *
Allow: /
Disallow: /originals/
Disallow: /.build/

# AI / answer-engine crawlers are explicitly welcome (Generative Engine
# Optimization). Functionally these are already allowed by the `*` group above;
# naming them is a statement of intent, and it means an AI-specific rule can be
# added later without touching the default group.
User-agent: GPTBot
User-agent: OAI-SearchBot
User-agent: ChatGPT-User
User-agent: ClaudeBot
User-agent: PerplexityBot
User-agent: Google-Extended
User-agent: Applebot-Extended
User-agent: CCBot
Allow: /
Disallow: /originals/
Disallow: /.build/

Sitemap: https://www.marketingpro-agency.com/sitemap.xml
